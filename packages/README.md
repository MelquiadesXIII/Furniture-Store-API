# packages/

Paquetes compartidos del workspace. Aún vacío — reservado para:

- `api-client/` — cliente TypeScript generado desde el OpenAPI de `apps/api`.
- `typescript-config/` — `tsconfig` base compartido entre apps y paquetes.
- `eslint-config/` — configuración ESLint compartida.
- `ui/` — componentes React compartidos.
